model_defaults:
  dialect: spark

gateways:
  spark_local:
    connection:
      type: spark
    state_connection:
      type: duckdb
      database: sqlmesh_state.duckdb

default_gateway: spark_local
